/**
 * @fileoverview Defines interactions with the event database. Path is './api/events'
 */

const express = require('express');
const Event = require('../../models/Event');

const router = express.Router();


module.exports = router;
