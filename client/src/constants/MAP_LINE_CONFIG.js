/**
 * @fileoverview Exports an object containing the uniform configuration for
 * line rendering on the map.
 */

export default {
  layout: {
    'line-cap': 'round',
    'line-join': 'round',
  },
  paint: {
    'line-width': 1,
    'line-gap-width': 1,
  },
};
